Ознакомься с эндпойнтом получения данных пользователя.
.prompts\docs\api\Данные участника.md

Конфигурация страницы вывода данных пользователя должна быть здесь: config\pages\user-edit.json

На нее осуществляется переход в виде http://host/users/23747/edit
где 23747 - это id пользователя.
В рамках конфигурации страницы, его можно получить через макрос location (изучи как).

Запрос осуществляется через свойство dataFeed (серверная загрузка данных перед серверным рендером, см docs\data-feed-reference.md), таким же образом, как на странице config\pages\user.json (без изменений)

Визуализация должна быть построена на компонентах системы docs\components-reference.md
Скомпонуй страницу, типичную для редактирования пользователя.
Ниже данные, которые нужно редактировать (поля сгруппированы по группам).

Личные данные:

Имя
Ввод строки

Фамилия
Ввод строки

E-mail
Ввод строки с валидацией

Телефон
Ввод строки с валидацией

Адрес доставки:
Адрес
Ввод строки

Комментарий
Ввод текста

Группа
Выбрать
Мульти-выбор групп участника

Статус:
Заблокировать
Переключатель

Причина
Ввод текста

Панель кнопок:
Сохранить
Отменить изменения

Отправка данных осуществляется на роут users/[id], метод PATH.
Для подготовки данных возможно нужен адаптер для конвертации полученных данных (request) пользователя (docs\data-adapter-reference.md).

После успешного сохранения или ошибки, должен вывестись тост.

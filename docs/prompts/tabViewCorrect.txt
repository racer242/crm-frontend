1. Возвращаем рендер TabPanel в renderTabView. иначе не происходит рендер.
2. Надо рендерить компоненты на панели как в блоке:
const content = (
    <div className="flex flex-column gap-3">
      {components
        .filter((c) => c !== null && c !== undefined)
        .map((component) => (
          <ComponentRenderer
            key={component.id}
            component={component}
            pageId={pageId || ""}
            appConfig={appConfig}
            stateManager={stateManager}
            elementIndex={elementIndex}
            showToast={showToast}
            navigate={navigate}
            confirm={confirm}
          />
        ))}
    </div>
  );
а затем рендерим панель
 <TabPanel {...tab.props}>
            {content}
          </TabPanel>

2. Теперь панель не будет являться BlockRenderer. Из BlockRenderer надо убрать TabPanel и AccordionTab
3. Аналогично с Accordion
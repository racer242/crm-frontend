Работа с url параметрами

1. src\app\[[...slug]]\page.tsx: pageConfig нужно скопировать "глубоко", чтобы изменения не коснулись глобальной конфигурации.
2. Сразу после получения pageConfig, нужно пройти по всем свойствам state в pageConfig, найти в значениях макросы и подставить их.

Багфикс.
Теперь elementIndex стал неверным.
Нам нужно:
1. config.ts,  initApp - убираем elementIndex из результата функции. Там остается только config.
2. page.tsx, сразу после const pageConfig = structuredClone(getPageConfigByRoute(route) ?? {}); создаем const elementIndex = buildFullIndex(config);

Таким образом, elementIndex становится актуальным

Обновление параметров.
1. Надо добавить команду, которая будет устанавливать параметры url-строки. 
2. Надо добавить команду, которая будет модифицировать определенный параметр url-строки. 

Управление форматом данных команд.
Добавь движок форматирования данных FormatEngine
Он должен реализовывыать основные функции Intl по работе с разными типами данных.
Настройка локали должна передаваться из config (определи место и свойство)

Затем, надо внедрить использование движка в командах
В конфигурации будет в виде:
{
  "type": "КОМАНДА",
  "params": {
    "prop1": "data",
    "prop2": "data",
    "format": [{
      prop:"prop1",
      type:"Date",
      func:"DateTimeFormat",
      params:{
        dateStyle: "full",
        timeStyle: "medium"
      }
    },
    ...
    ]
  }
}
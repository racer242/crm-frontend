services:
  crm-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crm-frontend
    ports:
      - "${HOST_PORT:-3028}:3000"
    env_file:
      - .env.production
    volumes:
      - ./config:/app/config
      - ./messages:/app/messages
    restart: unless-stopped

services:
  crm-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crm-frontend
    ports:
      - "127.0.0.1:${HOST_PORT:-3030}:3000"
    env_file:
      - .env.production
    volumes:
      - ./config:/app/config
      - ./messages:/app/messages
    restart: unless-stopped
